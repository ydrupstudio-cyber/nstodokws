# NS Pet Club — 미니게임

할일을 기록하고 공유할수록 점수가 쌓이고, 그 점수로 펫을 키우고 먹이는 기능입니다.

## 설치 (Supabase SQL Editor 에 순서대로)

```
1) SUPABASE_SQL_V7_PET.sql     점수 · 출석 · 원장 · 신고
2) SUPABASE_SQL_V8_FEED.sql    먹이 · 친밀도 · 미니룸 · 상점
3) SUPABASE_SQL_V9_HOTFIX.sql  game_grant 를 PUBLIC 에서 잠근다 (보안)
4) SUPABASE_SQL_V10_SHOP.sql   가구 상점 · 보관함
5) SUPABASE_SQL_V11_ADOPT_FIX.sql  해부 친구 입양 오류
6) SUPABASE_SQL_V12_BOND.sql   친밀도 성장 · 가방 · 착용 · 독립시키기
7) SUPABASE_SQL_V13_WEAR.sql   꾸미기 아이템을 상점에 올린다
```

순서를 바꾸면 실패합니다. 모든 파일이 여러 번 실행해도 안전합니다.

**V12 를 돌리기 전에는 방 화면의 가방·옷장·독립시키기가 동작하지 않습니다.**
간식이 상점에 안 보이고 가방이 비어 있으면 V12 가 아직 안 돌아간 것입니다.

## 구조

```
public/game/            에셋 339개 (SVG). manifest.json 이 모든 경로의 정답
  pet/  room/  gacha/
lib/pet/rig.js          부위별 SVG 조작 (디자인 킷 원본, 수정 금지)
lib/pet/motion.js       12가지 동작 (디자인 킷 원본, 수정 금지)
lib/pet/assets.js       manifest 로더 + SVG 캐시
lib/pet/room-engine.js  격자·충돌·경로 (디자인 킷 원본, 수정 금지)
lib/pet/room.js         씬 좌표·벽·깊이 정렬 보조
lib/game.js             서버 RPC 래퍼. 점수 계산을 여기서 하지 마라
app/PetCanvas.js        rig+motion 을 React 에 얹는 껍데기
app/PetView.js          방이 곧 화면인 껍데기. HUD · 친밀도 바 · 하단 바 · 서랍
app/RoomView.js         미니룸 본체 — 자율 행동, 가구 드래그 배치
app/PetPanels.js        서랍 — 가방 · 옷장 · 함께했던 친구들
app/ShopView.js         상점 — 가구 · 간식 · 꾸미기 · 벽지 · 방
app/AttendCalendar.js   출석 달력 (도장)
```

## 설계 원칙

**점수는 클라이언트가 못 만든다.** 점수표·상한·가격이 전부 DB 테이블에 있고,
`point_ledger` 와 `game_profiles` 에는 INSERT/UPDATE 정책이 없다.
`SECURITY DEFINER` 함수만 쓸 수 있다. 클라이언트는 "이런 일이 있었다"고 알릴 뿐이고,
줄지 말지는 서버가 실제 `todos`·`comments` 행을 확인해서 정한다.

**펫은 배고파지지 않는다.** 허기 게이지도, 줄어드는 수치도 만들지 마라.
당직 서고 이틀 만에 돌아온 사람에게 죄책감을 주면 안 된다.
먹이는 의무가 아니라 기회다. 친밀도는 절대 안 내려간다.

**성장은 점수가 아니라 친밀도가 정한다 (V12).** 누적 점수를 아무리 쌓아도 크지 않는다.
먹이고 돌본 만큼 자란다. 기준선은 친밀도 200 / 800 / 2000 / 4500 이고,
계산은 `game_stage()` 한 곳에 있다. 클라이언트의 `stageOf()` 는 그 복사본이니
**한쪽만 고치면 화면과 서버가 다른 단계를 말하게 된다.**

**간식은 사는 것과 먹이는 것이 분리돼 있다.** 상점에서 사면 `pet_inventory` 에 들어가고,
방에서 꺼내 먹인다. 먹일 때는 점수가 들지 않는다 — 이미 살 때 냈다.

**독립시키기는 파양이 아니다.** 방·가구·벽지·옷·점수는 그대로 두고 펫만 새로 온다.
떠난 친구는 `pet_history` 에 남는다. 지우는 함수가 아니다.

**에셋 경로는 manifest 로만 찾는다.** 파일명 규칙을 직접 조립하지 마라 —
어른 단계만 `growth/` 가 아니라 베이스 파일을 쓰는 등 예외가 있다.

## 미니룸 좌표 규약

킷 인계서 기준이다. **바꾸지 마라.** 가구·펫 위치가 통째로 어긋난다.

```
screenX = N*32 + (gx-gy)*32
screenY = 112 + (gx+gy)*16
가구 SVG  viewBox 0 0 256 224, 바닥 기준점 (128,164)
펫  SVG  viewBox 0 0 200 190, 바닥 기준선 y=176
```

정적 에셋(가구·바닥·벽지)은 `<image href>` 로 그린다. 펫만 인라인하는 이유는
부위별로 움직여야 하기 때문이다. 씬 안에서는 `foreignObject` 가 아니라
**중첩 `<svg>`** 를 쓴다 (PetCanvas 의 `embedded` 모드).

⚠️ 전역 `svg { width: ... }` 같은 CSS 를 추가하지 마라. 중첩된 펫 svg 의
width 속성을 덮어써서 방 안에서 펫만 거대해진다. 실제로 겪은 사고다.

## 아직 안 만든 것

- 가구 상점 (지금은 저장된 가구만 옮길 수 있다. 구매·인벤토리 없음)
- 벽지 구매 UI (`game_buy` 는 준비돼 있음)
- 방 확장 UI (`game_buy` 의 room-1 / room-2)
- 뽑기 상자 로직
- 칭찬 보내기 / 친구 방 구경
- 의국 전체 목표 판정
- 멤버 PIN (지금은 남의 계정으로 점수 요청이 가능하다. 활동 피드+신고로 막는 중)


## 교감 (RoomView)

```
방을 열면      친밀도 0 은 쳐다만 보고, 1 이상이면 앞으로 걸어 나와 인사한다.
              3 이상이면 기뻐한다 (celebrate). 머리 위에 하트가 뜬다.
펫을 누르면    쓰다듬기 — 손을 흔들거나 기뻐한다. 1.4초 쿨다운이 있다.
              점수도 친밀도도 오르지 않는다. 그냥 교감이다.
바닥을 누르면  그 칸으로 걸어온다. 갈 길이 없으면 알려준다.
가구를 누르면  그 가구를 쓰러 간다 (앉기·낮잠·놀기·살펴보기).
```

**가구 위에 올라간다.** `PERCH_LIFT` 에 적힌 가구는 옆 칸이 아니라 그 위에 선다.
값은 바닥에서 앉는 면까지의 높이(씬 단위)이고, manifest 에 없는 값이라 눈으로 맞췄다.
가구를 새로 받으면 여기에 한 줄 더하면 된다.

올라설 칸은 점유 칸 중 **가장 앞쪽**(x+w-1, y+h-1)이다. 뒤쪽 칸을 쓰면
깊이 정렬에서 펫이 가구 뒤로 숨는다.

**`EXTRA_INTERACTION` 은 킷 manifest 의 빈 칸을 메운다.** 방석·빈백·의자·러그처럼
`interaction` 이 비어 있던 가구에 행동을 붙였다. manifest 자체는 손대지 않는다 —
킷을 다시 납품받으면 덮어써지기 때문이다.

## 겪은 함정 (다시 밟지 마라)

**전역 `svg { width: ... }` CSS 가 중첩 펫 svg 의 width 속성을 덮어쓴다.**
방 안에서 펫만 거대해진다. PetCanvas 가 인라인 스타일로 한 번 더 못박아 뒀다.

**방 안 펫 크기는 `petScale` 하나로 정한다.** 0.62/0.72 로 뒀더니
아기가 한 칸의 절반도 안 돼 화면에서 안 보였다 (실측 26px).
지금은 단계별 `[0.95, 1.00, 1.06, 1.12, 1.15]` 다.

**서랍(가방·옷장…)은 하단 바를 덮으면 안 된다.** 덮이면 열어 놓은 채로
다른 서랍으로 못 옮겨 간다. PetView 가 하단 바 높이를 실측해 `bottom` 으로 넘긴다.

**`ui/icon-*.svg` 는 manifest 에 적혀 있지만 파일이 안 왔다 (404).**
UI 아이콘이 필요하면 인라인 SVG 로 그리거나 에셋을 먼저 받아라.

**쓰다듬기 판을 크게 잡으면 뒤쪽 가구를 못 누른다.** 펫 그림은 `aria-hidden` 이라
투명한 `<rect>` 를 덮어 두는데, 처음엔 펫 상자 전체를 덮었더니 그 뒤의 침대를
누를 수가 없었다. 지금은 킷 규약(x=100 중심, 바닥선 y=176)에 맞춰 아랫도리만 덮고,
펫이 서 있는 칸을 누르는 것도 쓰다듬기로 친다.

**Supabase SQL 에디터는 붙여넣은 전체를 한 트랜잭션으로 돌린다.**
맨 끝 검증문이 실패하면 앞의 함수 수정까지 통째로 롤백된다. 실제로 한 번 날아갔다.
